/**
 * Common types for blockchain-related services
 */

/**
 * Supported blockchain networks
 */
export enum BlockchainNetwork {
    ETHEREUM = "ethereum",
    BSC = "bsc",
    POLYGON = "polygon",
    SOLANA = "solana",
    ARBITRUM = "arbitrum",
    OPTIMISM = "optimism",
    AVALANCHE = "avalanche",
    BASE = "base"
}
